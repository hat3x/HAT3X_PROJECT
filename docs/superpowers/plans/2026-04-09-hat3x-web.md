# HAT3X Web — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir la web de marketing premium de HAT3X — dark inmersivo, animaciones elegantes, optimizada para conversión B2B con Next.js 14 + Framer Motion.

**Architecture:** Single-page marketing site con App Router de Next.js. Cada sección es un Server Component independiente salvo las que requieren animaciones (Client Components con `"use client"`). El copy vive en `lib/constants.ts` para ser mantenible sin tocar JSX. Motion system centralizado en `components/motion/`.

**Tech Stack:** Next.js 14 (App Router), Tailwind CSS v3, Framer Motion v11, TypeScript strict, Vitest + Testing Library, Vercel

---

## File Map

```
hat3x-web/
├── app/
│   ├── layout.tsx              # Root layout: fuentes, metadata, providers
│   ├── page.tsx                # Homepage: ensambla todas las secciones
│   └── globals.css             # CSS variables del design system + Tailwind base
├── components/
│   ├── ui/
│   │   ├── Button.tsx          # Variantes: primary, secondary, ghost
│   │   ├── Badge.tsx           # Badge con dot animado (hero badge)
│   │   └── SectionLabel.tsx    # Eyebrow label de sección (uppercase + color)
│   ├── motion/
│   │   ├── variants.ts         # Framer Motion variants reutilizables
│   │   ├── FadeInUp.tsx        # Wrapper: fadeInUp on scroll
│   │   └── StaggerChildren.tsx # Wrapper: stagger animación de hijos
│   ├── sections/
│   │   ├── Nav.tsx             # Glassmorphism, sticky, scroll-aware
│   │   ├── Hero.tsx            # Centrado cinematográfico (Client)
│   │   ├── ValueBar.tsx        # 4 métricas con counter animation (Client)
│   │   ├── WhatWeDo.tsx        # Copy directo + pills (Client)
│   │   ├── Services.tsx        # Bento Grid 6 servicios (Client)
│   │   ├── Process.tsx         # Timeline 5 pasos animado (Client)
│   │   ├── Benefits.tsx        # Grid 4×2 diferenciales (Client)
│   │   ├── Cases.tsx           # 3 cards por sector (Client)
│   │   ├── CtaFinal.tsx        # CTA con glow pulsante (Client)
│   │   └── Footer.tsx          # Footer minimal dark
│   └── decorative/
│       ├── GridBackground.tsx  # Grid tecnológico (líneas finas)
│       └── GlowOrb.tsx         # Orb de color difuso (radial gradient)
├── lib/
│   └── constants.ts            # TODO copy, servicios, métricas, navegación
├── __tests__/
│   ├── ui/
│   │   ├── Button.test.tsx
│   │   └── Badge.test.tsx
│   └── sections/
│       ├── Nav.test.tsx
│       ├── Hero.test.tsx
│       ├── Services.test.tsx
│       └── CtaFinal.test.tsx
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── vitest.config.ts
```

---

## Task 1: Scaffolding del proyecto Next.js

**Files:**
- Create: `hat3x-web/` (directorio raíz del proyecto)
- Create: `hat3x-web/next.config.ts`
- Create: `hat3x-web/tailwind.config.ts`
- Create: `hat3x-web/tsconfig.json`
- Create: `hat3x-web/vitest.config.ts`
- Create: `hat3x-web/app/globals.css`

- [ ] **Step 1: Crear el proyecto Next.js**

Ejecutar en `C:/Users/josem/Desktop/HAT3X/CLAUDE/HAT3X/`:

```bash
npx create-next-app@14 hat3x-web \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --no-src-dir \
  --import-alias "@/*"
```

Cuando pregunte sobre `Would you like to use Turbopack?` → responder **No** (compatibilidad estable).

- [ ] **Step 2: Instalar dependencias adicionales**

```bash
cd hat3x-web
npm install framer-motion lucide-react
npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

- [ ] **Step 3: Configurar vitest**

Crear `hat3x-web/vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./vitest.setup.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, '.'),
    },
  },
})
```

- [ ] **Step 4: Crear vitest setup**

Crear `hat3x-web/vitest.setup.ts`:

```ts
import '@testing-library/jest-dom'
```

- [ ] **Step 5: Actualizar package.json scripts**

En `hat3x-web/package.json`, añadir en `"scripts"`:

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 6: Verificar que el proyecto levanta**

```bash
npm run dev
```

Esperado: servidor en `http://localhost:3000` sin errores.

- [ ] **Step 7: Commit inicial**

```bash
git init
git add .
git commit -m "feat: scaffold Next.js 14 + Tailwind + Framer Motion + Vitest"
```

---

## Task 2: Design tokens y globals CSS

**Files:**
- Create: `hat3x-web/app/globals.css`
- Modify: `hat3x-web/tailwind.config.ts`

- [ ] **Step 1: Escribir CSS variables del design system**

Reemplazar `hat3x-web/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* Colors */
    --purple-primary: #7C3AED;
    --purple-light: #A78BFA;
    --orange-accent: #EA580C;
    --orange-light: #FB923C;

    /* Surfaces */
    --base-black: #040408;
    --surface-1: #07070F;
    --surface-2: #0B0B16;
    --surface-3: #111120;

    /* Borders */
    --border-subtle: #161626;
    --border-purple: rgba(124, 58, 237, 0.2);

    /* Text */
    --text-primary: #E2E2E8;
    --text-secondary: #888888;
    --text-muted: #555555;
    --text-ghost: #333333;
  }

  * {
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--surface-1);
    color: var(--text-primary);
    font-family: var(--font-inter), system-ui, sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Selection */
  ::selection {
    background: rgba(124, 58, 237, 0.3);
    color: #fff;
  }

  /* Scrollbar */
  ::-webkit-scrollbar {
    width: 6px;
  }
  ::-webkit-scrollbar-track {
    background: var(--base-black);
  }
  ::-webkit-scrollbar-thumb {
    background: var(--surface-3);
    border-radius: 3px;
  }
}
```

- [ ] **Step 2: Extender Tailwind config con los tokens**

Reemplazar `hat3x-web/tailwind.config.ts`:

```ts
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        'purple-primary': '#7C3AED',
        'purple-light': '#A78BFA',
        'orange-accent': '#EA580C',
        'orange-light': '#FB923C',
        'base-black': '#040408',
        'surface-1': '#07070F',
        'surface-2': '#0B0B16',
        'surface-3': '#111120',
        'border-subtle': '#161626',
        'text-primary': '#E2E2E8',
        'text-secondary': '#888888',
        'text-muted': '#555555',
        'text-ghost': '#333333',
      },
      fontFamily: {
        inter: ['var(--font-inter)', 'system-ui', 'sans-serif'],
      },
      letterSpacing: {
        'display': '-0.06em',
        'heading': '-0.05em',
        'subheading': '-0.04em',
        'card': '-0.02em',
        'eyebrow': '0.2em',
        'badge': '0.12em',
      },
      maxWidth: {
        'content': '1280px',
      },
      backgroundImage: {
        'purple-gradient': 'linear-gradient(135deg, #7C3AED 0%, #6D28D9 100%)',
        'brand-gradient': 'linear-gradient(90deg, #7C3AED 0%, #A855F7 50%, #EA580C 100%)',
        'glow-purple': 'radial-gradient(ellipse at 50% 0%, rgba(124,58,237,0.5) 0%, transparent 60%)',
      },
      animation: {
        'pulse-glow': 'pulseGlow 4s ease-in-out infinite',
        'dot-pulse': 'dotPulse 2s ease-in-out infinite',
      },
      keyframes: {
        pulseGlow: {
          '0%, 100%': { opacity: '0.6', transform: 'scale(1)' },
          '50%': { opacity: '1', transform: 'scale(1.05)' },
        },
        dotPulse: {
          '0%, 100%': { opacity: '1', boxShadow: '0 0 6px #EA580C' },
          '50%': { opacity: '0.5', boxShadow: '0 0 12px #EA580C' },
        },
      },
    },
  },
  plugins: [],
}

export default config
```

- [ ] **Step 3: Verificar build sin errores**

```bash
npm run build
```

Esperado: build exitoso, 0 errores TypeScript.

- [ ] **Step 4: Commit**

```bash
git add app/globals.css tailwind.config.ts
git commit -m "feat: design tokens CSS variables + Tailwind extensions"
```

---

## Task 3: Layout root y fuentes

**Files:**
- Modify: `hat3x-web/app/layout.tsx`

- [ ] **Step 1: Actualizar layout con Inter font y metadata**

Reemplazar `hat3x-web/app/layout.tsx`:

```tsx
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'HAT3X — Consultoría e Implementación de IA para Empresas',
  description: 'No tienes que aprender IA. HAT3X la implementa por ti. Soluciones reales de inteligencia artificial para tu negocio.',
  keywords: ['consultoría IA', 'inteligencia artificial', 'automatización', 'agentes IA', 'implementación IA'],
  openGraph: {
    title: 'HAT3X — Consultoría e Implementación de IA',
    description: 'No tienes que aprender IA. HAT3X la implementa por ti.',
    type: 'website',
    url: 'https://hat3x.com',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'HAT3X — Consultoría e Implementación de IA',
    description: 'No tienes que aprender IA. HAT3X la implementa por ti.',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="es" className={inter.variable}>
      <body className="bg-surface-1 text-text-primary antialiased">
        {children}
      </body>
    </html>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add app/layout.tsx
git commit -m "feat: root layout con Inter font y metadata SEO"
```

---

## Task 4: Constantes de copy

**Files:**
- Create: `hat3x-web/lib/constants.ts`

- [ ] **Step 1: Escribir todas las constantes de contenido**

Crear `hat3x-web/lib/constants.ts`:

```ts
export const NAV_LINKS = [
  { label: 'Servicios', href: '#servicios' },
  { label: 'Proceso', href: '#proceso' },
  { label: 'Casos', href: '#casos' },
  { label: 'Nosotros', href: '#nosotros' },
] as const

export const HERO = {
  badge: 'Implementación IA para empresas',
  h1Line1: 'No aprendes IA.',
  h1Line2: 'HAT3X la implementa.',
  subtitle: 'Soluciones reales de inteligencia artificial para tu negocio. Sin curvas de aprendizaje. Sin humo. Sin esperar.',
  ctaPrimary: 'Reservar llamada gratuita',
  ctaSecondary: 'Ver servicios',
  ctaPrimaryHref: 'https://cal.com/hat3x', // reemplazar con URL real
  ctaSecondaryHref: '#servicios',
} as const

export const VALUE_METRICS = [
  { value: '6', label: 'Verticales de IA', desc: 'Automatización, voz, chatbots, apps, analytics, gobernanza' },
  { value: '100', suffix: '%', label: 'Implementación real', desc: 'No consultoría vacía. Entregamos sistemas funcionales' },
  { value: 'B2B', label: 'Enfoque empresarial', desc: 'Para pymes y empresas que necesitan resultados medibles' },
  { value: '0', label: 'Curva de aprendizaje', desc: 'Tu equipo no necesita saber IA. Nosotros la operamos' },
] as const

export const WHAT_WE_DO = {
  eyebrow: 'Qué hacemos',
  title: 'Implementamos IA que funciona en tu negocio hoy.',
  pills: [
    'Automatización de procesos',
    'Agentes de voz',
    'Chatbots inteligentes',
    'Apps con IA',
    'Análisis de datos',
    'Integraciones',
  ],
} as const

export const SERVICES = [
  {
    id: '01',
    icon: '🔍',
    title: 'Auditoría y Roadmap de IA',
    desc: 'Analizamos tu negocio, identificamos los procesos con mayor ROI potencial y diseñamos un plan de implementación realista y priorizado. El primer paso antes de cualquier solución.',
    tags: ['Diagnóstico completo', '2–4 semanas', 'Entregable PDF'],
    featured: true,
  },
  {
    id: '02',
    icon: '⚡',
    title: 'Automatización y Agentes',
    desc: 'Workflows inteligentes que eliminan tareas repetitivas. Agentes que actúan por tu equipo 24/7.',
    tags: ['n8n · Make · Custom'],
    featured: false,
  },
  {
    id: '03',
    icon: '📱',
    title: 'Apps y Productos con IA',
    desc: 'Desarrollamos productos digitales con IA integrada. Desde MVPs hasta plataformas completas.',
    tags: ['Next.js · Supabase'],
    featured: false,
  },
  {
    id: '04',
    icon: '🔗',
    title: 'Integraciones',
    desc: 'Conectamos tus sistemas existentes — CRM, ERP, ecommerce, BBDD — con capa de IA encima.',
    tags: ['APIs · Webhooks · CRM'],
    featured: false,
  },
  {
    id: '05',
    icon: '📊',
    title: 'Analítica y ML',
    desc: 'Modelos predictivos, dashboards inteligentes y análisis de datos para decisiones basadas en evidencia.',
    tags: ['Python · SQL · BI'],
    featured: false,
  },
  {
    id: '06',
    icon: '🛡️',
    title: 'Gobernanza y Adopción',
    desc: 'Implementamos IA de forma responsable y aseguramos que tu equipo la adopte con éxito.',
    tags: ['Estrategia · Change Mgmt'],
    featured: false,
  },
] as const

export const PROCESS_STEPS = [
  { num: '01', label: 'Diagnóstico', desc: 'Analizamos tu negocio y procesos actuales' },
  { num: '02', label: 'Roadmap', desc: 'Plan priorizado por impacto y ROI' },
  { num: '03', label: 'Implementación', desc: 'Desarrollo y despliegue de la solución' },
  { num: '04', label: 'Integración', desc: 'Conexión con tus sistemas existentes' },
  { num: '05', label: 'Optimización', desc: 'Mejora continua basada en resultados' },
] as const

export const BENEFITS = [
  'Resultados medibles desde el día 1',
  'Sin dependencias de tu equipo',
  'Tecnología real, no demos',
  'Escalable con tu negocio',
  'Sin curva de aprendizaje interna',
  'Stack moderno y seguro',
  'Soporte y mantenimiento continuo',
  'Transparencia total en el proceso',
] as const

export const CASES = [
  {
    sector: 'Hostelería',
    metric: '-70%',
    title: 'Gestión de reservas automatizada',
    desc: 'Agente de voz 24/7 con integración CRM. Cero llamadas perdidas.',
  },
  {
    sector: 'Retail / E-commerce',
    metric: '3× ROI',
    title: 'Atención al cliente con IA',
    desc: 'Chatbot multicanal con respuesta instantánea en WhatsApp, web y email.',
  },
  {
    sector: 'Servicios B2B',
    metric: '+40%',
    title: 'Pipeline de ventas inteligente',
    desc: 'Automatización de leads, seguimiento y calificación sin intervención manual.',
  },
] as const

export const CTA_FINAL = {
  title: '¿Listo para implementar IA real?',
  subtitle: 'Primera llamada gratuita · Sin compromiso · Resultados en semanas',
  ctaPrimary: 'Reservar llamada',
  ctaSecondary: 'Contactar por email',
  ctaPrimaryHref: 'https://cal.com/hat3x', // reemplazar con URL real
  ctaSecondaryHref: 'mailto:hola@hat3x.com', // reemplazar con email real
} as const

export const FOOTER = {
  tagline: 'Consultoría e Implementación de IA para Empresas',
  links: [
    { label: 'Privacidad', href: '/privacidad' },
    { label: 'Términos', href: '/terminos' },
    { label: 'Contacto', href: 'mailto:hola@hat3x.com' },
  ],
  copy: '© 2025 HAT3X · Todos los derechos reservados',
} as const
```

- [ ] **Step 2: Commit**

```bash
git add lib/constants.ts
git commit -m "feat: constants de copy — todo el contenido en un lugar"
```

---

## Task 5: Motion system (variants + wrappers)

**Files:**
- Create: `hat3x-web/components/motion/variants.ts`
- Create: `hat3x-web/components/motion/FadeInUp.tsx`
- Create: `hat3x-web/components/motion/StaggerChildren.tsx`
- Create: `hat3x-web/__tests__/motion/FadeInUp.test.tsx`

- [ ] **Step 1: Escribir test para FadeInUp**

Crear `hat3x-web/__tests__/motion/FadeInUp.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { FadeInUp } from '@/components/motion/FadeInUp'

describe('FadeInUp', () => {
  it('renders children', () => {
    render(
      <FadeInUp>
        <p>Test content</p>
      </FadeInUp>
    )
    expect(screen.getByText('Test content')).toBeInTheDocument()
  })

  it('accepts custom className', () => {
    render(
      <FadeInUp className="custom-class">
        <p>Content</p>
      </FadeInUp>
    )
    const container = screen.getByText('Content').parentElement
    expect(container).toHaveClass('custom-class')
  })

  it('accepts custom delay', () => {
    // Should not throw when delay prop is provided
    expect(() =>
      render(
        <FadeInUp delay={0.5}>
          <p>Content</p>
        </FadeInUp>
      )
    ).not.toThrow()
  })
})
```

- [ ] **Step 2: Ejecutar test — verificar que falla**

```bash
npm run test
```

Esperado: FAIL — `Cannot find module '@/components/motion/FadeInUp'`

- [ ] **Step 3: Crear variants.ts**

Crear `hat3x-web/components/motion/variants.ts`:

```ts
import type { Variants } from 'framer-motion'

export const fadeInUpVariants: Variants = {
  hidden: { opacity: 0, y: 24 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 0.5,
      ease: [0.16, 1, 0.3, 1],
    },
  },
}

export const fadeInVariants: Variants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { duration: 0.8 },
  },
}

export const staggerContainerVariants: Variants = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.09,
      delayChildren: 0.1,
    },
  },
}

export const pulseVariants: Variants = {
  animate: {
    scale: [1, 1.05, 1],
    opacity: [0.6, 1, 0.6],
    transition: {
      duration: 4,
      ease: 'easeInOut',
      repeat: Infinity,
    },
  },
}

export const drawLineVariants: Variants = {
  hidden: { scaleX: 0 },
  visible: {
    scaleX: 1,
    transition: { duration: 1.2, ease: 'easeOut' },
  },
}
```

- [ ] **Step 4: Crear FadeInUp.tsx**

Crear `hat3x-web/components/motion/FadeInUp.tsx`:

```tsx
'use client'

import { motion, useInView } from 'framer-motion'
import { useRef } from 'react'
import { fadeInUpVariants } from './variants'

interface FadeInUpProps {
  children: React.ReactNode
  className?: string
  delay?: number
}

export function FadeInUp({ children, className, delay = 0 }: FadeInUpProps) {
  const ref = useRef<HTMLDivElement>(null)
  const isInView = useInView(ref, { once: true, margin: '-10% 0px' })

  return (
    <motion.div
      ref={ref}
      className={className}
      variants={fadeInUpVariants}
      initial="hidden"
      animate={isInView ? 'visible' : 'hidden'}
      transition={{ delay }}
    >
      {children}
    </motion.div>
  )
}
```

- [ ] **Step 5: Crear StaggerChildren.tsx**

Crear `hat3x-web/components/motion/StaggerChildren.tsx`:

```tsx
'use client'

import { motion, useInView } from 'framer-motion'
import { useRef } from 'react'
import { staggerContainerVariants, fadeInUpVariants } from './variants'

interface StaggerChildrenProps {
  children: React.ReactNode
  className?: string
  childClassName?: string
}

export function StaggerChildren({ children, className, childClassName }: StaggerChildrenProps) {
  const ref = useRef<HTMLDivElement>(null)
  const isInView = useInView(ref, { once: true, margin: '-10% 0px' })

  return (
    <motion.div
      ref={ref}
      className={className}
      variants={staggerContainerVariants}
      initial="hidden"
      animate={isInView ? 'visible' : 'hidden'}
    >
      {Array.isArray(children)
        ? children.map((child, i) => (
            <motion.div key={i} variants={fadeInUpVariants} className={childClassName}>
              {child}
            </motion.div>
          ))
        : children}
    </motion.div>
  )
}
```

- [ ] **Step 6: Ejecutar tests — verificar que pasan**

```bash
npm run test
```

Esperado: PASS — 3 tests pasan.

- [ ] **Step 7: Commit**

```bash
git add components/motion/ __tests__/motion/
git commit -m "feat: motion system — variants + FadeInUp + StaggerChildren"
```

---

## Task 6: Componentes UI primitivos

**Files:**
- Create: `hat3x-web/components/ui/Button.tsx`
- Create: `hat3x-web/components/ui/Badge.tsx`
- Create: `hat3x-web/components/ui/SectionLabel.tsx`
- Create: `hat3x-web/__tests__/ui/Button.test.tsx`
- Create: `hat3x-web/__tests__/ui/Badge.test.tsx`

- [ ] **Step 1: Escribir tests de Button**

Crear `hat3x-web/__tests__/ui/Button.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { Button } from '@/components/ui/Button'

describe('Button', () => {
  it('renders primary variant by default', () => {
    render(<Button>Click me</Button>)
    const btn = screen.getByRole('button', { name: /click me/i })
    expect(btn).toBeInTheDocument()
    expect(btn).toHaveClass('bg-purple-primary')
  })

  it('renders secondary variant', () => {
    render(<Button variant="secondary">Secondary</Button>)
    const btn = screen.getByRole('button', { name: /secondary/i })
    expect(btn).toHaveClass('border-border-subtle')
  })

  it('renders as anchor when href is provided', () => {
    render(<Button href="/test">Link button</Button>)
    const link = screen.getByRole('link', { name: /link button/i })
    expect(link).toHaveAttribute('href', '/test')
  })

  it('is accessible — has visible text', () => {
    render(<Button>Reservar llamada</Button>)
    expect(screen.getByText('Reservar llamada')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Ejecutar test — verificar que falla**

```bash
npm run test -- __tests__/ui/Button.test.tsx
```

Esperado: FAIL — `Cannot find module '@/components/ui/Button'`

- [ ] **Step 3: Implementar Button**

Crear `hat3x-web/components/ui/Button.tsx`:

```tsx
import Link from 'next/link'
import { cn } from '@/lib/utils'

type ButtonVariant = 'primary' | 'secondary' | 'ghost'

interface ButtonProps {
  children: React.ReactNode
  variant?: ButtonVariant
  href?: string
  onClick?: () => void
  className?: string
  external?: boolean
}

const variants: Record<ButtonVariant, string> = {
  primary: [
    'bg-purple-primary hover:bg-[#6D28D9]',
    'text-white font-bold',
    'shadow-[0_4px_20px_rgba(124,58,237,0.4)]',
    'hover:shadow-[0_4px_28px_rgba(124,58,237,0.55)]',
  ].join(' '),
  secondary: [
    'bg-white/[0.03] hover:bg-white/[0.06]',
    'border border-border-subtle hover:border-[#2a2a3a]',
    'text-text-secondary hover:text-text-primary',
  ].join(' '),
  ghost: [
    'text-text-secondary hover:text-text-primary',
  ].join(' '),
}

const base = 'inline-flex items-center justify-center gap-2 rounded-lg px-6 py-2.5 text-sm font-semibold transition-all duration-200 cursor-pointer'

export function Button({ children, variant = 'primary', href, onClick, className, external }: ButtonProps) {
  const classes = cn(base, variants[variant], className)

  if (href) {
    return (
      <Link
        href={href}
        className={classes}
        {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
      >
        {children}
      </Link>
    )
  }

  return (
    <button onClick={onClick} className={classes}>
      {children}
    </button>
  )
}
```

- [ ] **Step 4: Crear lib/utils.ts**

Crear `hat3x-web/lib/utils.ts`:

```ts
import { type ClassValue, clsx } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

Instalar dependencias:

```bash
npm install clsx tailwind-merge
```

- [ ] **Step 5: Implementar Badge**

Crear `hat3x-web/__tests__/ui/Badge.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { Badge } from '@/components/ui/Badge'

describe('Badge', () => {
  it('renders text content', () => {
    render(<Badge>Implementación IA</Badge>)
    expect(screen.getByText('Implementación IA')).toBeInTheDocument()
  })

  it('has accessible role', () => {
    render(<Badge>Status badge</Badge>)
    expect(screen.getByText('Status badge')).toBeInTheDocument()
  })
})
```

Crear `hat3x-web/components/ui/Badge.tsx`:

```tsx
import { cn } from '@/lib/utils'

interface BadgeProps {
  children: React.ReactNode
  className?: string
}

export function Badge({ children, className }: BadgeProps) {
  return (
    <span
      className={cn(
        'inline-flex items-center gap-2',
        'bg-purple-primary/10 border border-purple-primary/20',
        'text-purple-light text-[0.625rem] font-semibold uppercase tracking-badge',
        'px-4 py-1.5 rounded-full',
        className
      )}
    >
      {/* Dot naranja pulsante */}
      <span className="w-1.5 h-1.5 rounded-full bg-orange-accent animate-dot-pulse flex-shrink-0" />
      {children}
    </span>
  )
}
```

- [ ] **Step 6: Implementar SectionLabel**

Crear `hat3x-web/components/ui/SectionLabel.tsx`:

```tsx
import { cn } from '@/lib/utils'

interface SectionLabelProps {
  children: React.ReactNode
  color?: 'purple' | 'orange'
  className?: string
}

export function SectionLabel({ children, color = 'purple', className }: SectionLabelProps) {
  return (
    <p
      className={cn(
        'text-[0.625rem] font-bold uppercase tracking-eyebrow',
        color === 'purple' ? 'text-purple-light' : 'text-orange-accent',
        className
      )}
    >
      {children}
    </p>
  )
}
```

- [ ] **Step 7: Ejecutar todos los tests**

```bash
npm run test
```

Esperado: PASS — todos los tests.

- [ ] **Step 8: Commit**

```bash
git add components/ui/ lib/ __tests__/ui/
git commit -m "feat: UI primitives — Button, Badge, SectionLabel + utils"
```

---

## Task 7: Componentes decorativos

**Files:**
- Create: `hat3x-web/components/decorative/GridBackground.tsx`
- Create: `hat3x-web/components/decorative/GlowOrb.tsx`

- [ ] **Step 1: Crear GridBackground**

Crear `hat3x-web/components/decorative/GridBackground.tsx`:

```tsx
import { cn } from '@/lib/utils'

interface GridBackgroundProps {
  className?: string
  opacity?: number
  size?: number
  maskFrom?: string // radial-gradient mask direction
}

export function GridBackground({
  className,
  opacity = 0.07,
  size = 50,
  maskFrom = '50% 0%',
}: GridBackgroundProps) {
  return (
    <div
      aria-hidden="true"
      className={cn('absolute inset-0 pointer-events-none', className)}
      style={{
        backgroundImage: `
          linear-gradient(rgba(124,58,237,${opacity}) 1px, transparent 1px),
          linear-gradient(90deg, rgba(124,58,237,${opacity}) 1px, transparent 1px)
        `,
        backgroundSize: `${size}px ${size}px`,
        maskImage: `radial-gradient(ellipse at ${maskFrom}, black 20%, transparent 75%)`,
        WebkitMaskImage: `radial-gradient(ellipse at ${maskFrom}, black 20%, transparent 75%)`,
      }}
    />
  )
}
```

- [ ] **Step 2: Crear GlowOrb**

Crear `hat3x-web/components/decorative/GlowOrb.tsx`:

```tsx
import { cn } from '@/lib/utils'

interface GlowOrbProps {
  color?: 'purple' | 'orange'
  size?: number // px
  opacity?: number
  blur?: number // px
  className?: string
}

export function GlowOrb({
  color = 'purple',
  size = 400,
  opacity = 0.35,
  blur = 60,
  className,
}: GlowOrbProps) {
  const colorMap = {
    purple: `rgba(124,58,237,${opacity})`,
    orange: `rgba(234,88,12,${opacity})`,
  }

  return (
    <div
      aria-hidden="true"
      className={cn('absolute pointer-events-none rounded-full animate-pulse-glow', className)}
      style={{
        width: size,
        height: size,
        background: `radial-gradient(circle, ${colorMap[color]} 0%, transparent 70%)`,
        filter: `blur(${blur}px)`,
      }}
    />
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add components/decorative/
git commit -m "feat: decorative components — GridBackground, GlowOrb"
```

---

## Task 8: Navegación

**Files:**
- Create: `hat3x-web/components/sections/Nav.tsx`
- Create: `hat3x-web/__tests__/sections/Nav.test.tsx`

- [ ] **Step 1: Escribir test de Nav**

Crear `hat3x-web/__tests__/sections/Nav.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { Nav } from '@/components/sections/Nav'

describe('Nav', () => {
  it('renders logo', () => {
    render(<Nav />)
    expect(screen.getByText('HAT3X')).toBeInTheDocument()
  })

  it('renders navigation links', () => {
    render(<Nav />)
    expect(screen.getByRole('link', { name: /servicios/i })).toBeInTheDocument()
    expect(screen.getByRole('link', { name: /proceso/i })).toBeInTheDocument()
  })

  it('renders primary CTA', () => {
    render(<Nav />)
    expect(screen.getByRole('link', { name: /reservar llamada/i })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Ejecutar test — verificar que falla**

```bash
npm run test -- __tests__/sections/Nav.test.tsx
```

Esperado: FAIL

- [ ] **Step 3: Implementar Nav**

Crear `hat3x-web/components/sections/Nav.tsx`:

```tsx
'use client'

import { useState, useEffect } from 'react'
import Link from 'next/link'
import { motion } from 'framer-motion'
import { cn } from '@/lib/utils'
import { NAV_LINKS, HERO } from '@/lib/constants'

export function Nav() {
  const [scrolled, setScrolled] = useState(false)
  const [mobileOpen, setMobileOpen] = useState(false)

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 80)
    window.addEventListener('scroll', handleScroll, { passive: true })
    return () => window.removeEventListener('scroll', handleScroll)
  }, [])

  return (
    <motion.header
      initial={{ opacity: 0, y: -16 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
      className={cn(
        'fixed top-0 left-0 right-0 z-50 transition-all duration-300',
        scrolled
          ? 'bg-surface-1/85 backdrop-blur-xl border-b border-white/5'
          : 'bg-transparent'
      )}
    >
      <div className="max-w-content mx-auto px-6 h-16 flex items-center gap-8">
        {/* Logo */}
        <Link href="/" className="text-lg font-black tracking-card text-white flex-shrink-0">
          HAT3X
        </Link>

        {/* Desktop nav */}
        <nav className="hidden md:flex items-center gap-6 ml-4" aria-label="Navegación principal">
          {NAV_LINKS.map((link) => (
            <Link
              key={link.href}
              href={link.href}
              className="text-sm text-text-muted hover:text-text-primary transition-colors duration-200"
            >
              {link.label}
            </Link>
          ))}
        </nav>

        {/* CTA */}
        <div className="ml-auto flex items-center gap-3">
          <Link
            href={HERO.ctaPrimaryHref}
            target="_blank"
            rel="noopener noreferrer"
            className={cn(
              'hidden md:inline-flex items-center gap-2',
              'bg-purple-primary hover:bg-[#6D28D9]',
              'text-white text-sm font-bold',
              'px-4 py-2 rounded-lg',
              'transition-all duration-200',
              'shadow-[0_4px_20px_rgba(124,58,237,0.35)]'
            )}
          >
            Reservar llamada →
          </Link>

          {/* Mobile hamburger */}
          <button
            className="md:hidden text-text-muted hover:text-white p-1"
            onClick={() => setMobileOpen(!mobileOpen)}
            aria-label="Abrir menú"
          >
            <div className="w-5 h-0.5 bg-current mb-1.5 transition-all" />
            <div className="w-5 h-0.5 bg-current mb-1.5" />
            <div className="w-5 h-0.5 bg-current" />
          </button>
        </div>
      </div>

      {/* Mobile menu */}
      {mobileOpen && (
        <motion.div
          initial={{ opacity: 0, height: 0 }}
          animate={{ opacity: 1, height: 'auto' }}
          exit={{ opacity: 0, height: 0 }}
          className="md:hidden bg-surface-2/95 backdrop-blur-xl border-t border-border-subtle px-6 py-4 flex flex-col gap-4"
        >
          {NAV_LINKS.map((link) => (
            <Link
              key={link.href}
              href={link.href}
              className="text-sm text-text-secondary hover:text-white py-1"
              onClick={() => setMobileOpen(false)}
            >
              {link.label}
            </Link>
          ))}
          <Link
            href={HERO.ctaPrimaryHref}
            className="bg-purple-primary text-white text-sm font-bold px-4 py-2.5 rounded-lg text-center mt-2"
            onClick={() => setMobileOpen(false)}
          >
            Reservar llamada →
          </Link>
        </motion.div>
      )}
    </motion.header>
  )
}
```

- [ ] **Step 4: Ejecutar tests — verificar que pasan**

```bash
npm run test -- __tests__/sections/Nav.test.tsx
```

Esperado: PASS

- [ ] **Step 5: Commit**

```bash
git add components/sections/Nav.tsx __tests__/sections/Nav.test.tsx
git commit -m "feat: Nav — glassmorphism sticky scroll-aware + mobile"
```

---

## Task 9: Hero Section

**Files:**
- Create: `hat3x-web/components/sections/Hero.tsx`
- Create: `hat3x-web/__tests__/sections/Hero.test.tsx`

- [ ] **Step 1: Escribir test de Hero**

Crear `hat3x-web/__tests__/sections/Hero.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { Hero } from '@/components/sections/Hero'

describe('Hero', () => {
  it('renders main headline', () => {
    render(<Hero />)
    expect(screen.getByText('No aprendes IA.')).toBeInTheDocument()
    expect(screen.getByText('HAT3X la implementa.')).toBeInTheDocument()
  })

  it('renders badge', () => {
    render(<Hero />)
    expect(screen.getByText('Implementación IA para empresas')).toBeInTheDocument()
  })

  it('renders primary CTA with correct href', () => {
    render(<Hero />)
    const cta = screen.getByRole('link', { name: /reservar llamada/i })
    expect(cta).toHaveAttribute('href', expect.stringContaining('cal.com'))
  })

  it('renders secondary CTA', () => {
    render(<Hero />)
    expect(screen.getByRole('link', { name: /ver servicios/i })).toBeInTheDocument()
  })

  it('renders subtitle text', () => {
    render(<Hero />)
    expect(screen.getByText(/sin curvas de aprendizaje/i)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Ejecutar test — verificar que falla**

```bash
npm run test -- __tests__/sections/Hero.test.tsx
```

Esperado: FAIL

- [ ] **Step 3: Implementar Hero**

Crear `hat3x-web/components/sections/Hero.tsx`:

```tsx
'use client'

import { motion } from 'framer-motion'
import Link from 'next/link'
import { Badge } from '@/components/ui/Badge'
import { GridBackground } from '@/components/decorative/GridBackground'
import { GlowOrb } from '@/components/decorative/GlowOrb'
import { HERO } from '@/lib/constants'
import { fadeInUpVariants } from '@/components/motion/variants'

const NODES = [
  { top: '15%', left: '8%' },
  { top: '70%', left: '13%' },
  { top: '22%', right: '9%' },
  { top: '68%', right: '15%' },
  { top: '42%', left: '22%' },
  { top: '55%', right: '21%' },
]

export function Hero() {
  return (
    <section className="relative min-h-screen flex flex-col items-center justify-center overflow-hidden bg-base-black">
      {/* Decorative background */}
      <GridBackground maskFrom="50% 0%" />
      <GlowOrb color="purple" size={600} opacity={0.35} blur={80} className="-top-32 left-1/2 -translate-x-1/2" />
      <GlowOrb color="orange" size={300} opacity={0.15} blur={50} className="bottom-0 right-[5%]" />

      {/* Floating nodes */}
      <div aria-hidden="true" className="absolute inset-0 pointer-events-none">
        {NODES.map((pos, i) => (
          <motion.div
            key={i}
            className="absolute w-1.5 h-1.5 rounded-full border border-purple-primary/40 bg-purple-primary/15"
            style={pos}
            initial={{ opacity: 0, scale: 0 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ delay: 0.8 + i * 0.1, duration: 0.4 }}
          >
            <div className="absolute inset-0 m-auto w-0.5 h-0.5 rounded-full bg-purple-light" />
          </motion.div>
        ))}
      </div>

      {/* Content */}
      <div className="relative z-10 flex flex-col items-center gap-6 px-6 text-center max-w-3xl">
        {/* Badge */}
        <motion.div
          variants={fadeInUpVariants}
          initial="hidden"
          animate="visible"
          transition={{ delay: 0.1 }}
        >
          <Badge>{HERO.badge}</Badge>
        </motion.div>

        {/* H1 */}
        <motion.div
          variants={fadeInUpVariants}
          initial="hidden"
          animate="visible"
          transition={{ delay: 0.3 }}
        >
          <h1 className="text-5xl sm:text-6xl lg:text-7xl xl:text-8xl font-black tracking-display leading-none">
            <span className="text-white block">{HERO.h1Line1}</span>
            <span
              className="block"
              style={{
                background: 'linear-gradient(90deg, #7C3AED 0%, #A855F7 50%, #EA580C 100%)',
                WebkitBackgroundClip: 'text',
                WebkitTextFillColor: 'transparent',
                backgroundClip: 'text',
              }}
            >
              {HERO.h1Line2}
            </span>
          </h1>
        </motion.div>

        {/* Subtitle */}
        <motion.p
          className="text-base text-text-muted max-w-md leading-relaxed"
          variants={fadeInUpVariants}
          initial="hidden"
          animate="visible"
          transition={{ delay: 0.5 }}
        >
          {HERO.subtitle}
        </motion.p>

        {/* CTAs */}
        <motion.div
          className="flex flex-col sm:flex-row gap-3 mt-2"
          variants={fadeInUpVariants}
          initial="hidden"
          animate="visible"
          transition={{ delay: 0.7 }}
        >
          <Link
            href={HERO.ctaPrimaryHref}
            target="_blank"
            rel="noopener noreferrer"
            className="inline-flex items-center justify-center gap-2 bg-purple-primary hover:bg-[#6D28D9] text-white text-sm font-bold px-6 py-3 rounded-lg transition-all duration-200 shadow-[0_4px_24px_rgba(124,58,237,0.45)] hover:shadow-[0_4px_32px_rgba(124,58,237,0.6)]"
          >
            {HERO.ctaPrimary} →
          </Link>
          <Link
            href={HERO.ctaSecondaryHref}
            className="inline-flex items-center justify-center gap-2 bg-white/[0.03] border border-white/[0.08] hover:border-white/[0.15] text-text-secondary hover:text-text-primary text-sm font-semibold px-6 py-3 rounded-lg transition-all duration-200"
          >
            {HERO.ctaSecondary}
          </Link>
        </motion.div>
      </div>

      {/* Scroll indicator */}
      <motion.div
        className="absolute bottom-8 left-1/2 -translate-x-1/2"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 1.2, duration: 0.5 }}
      >
        <div className="flex flex-col items-center gap-2">
          <div className="w-px h-8 bg-gradient-to-b from-transparent via-purple-primary/40 to-transparent animate-pulse" />
        </div>
      </motion.div>
    </section>
  )
}
```

- [ ] **Step 4: Ejecutar tests**

```bash
npm run test -- __tests__/sections/Hero.test.tsx
```

Esperado: PASS — 5 tests.

- [ ] **Step 5: Commit**

```bash
git add components/sections/Hero.tsx __tests__/sections/Hero.test.tsx
git commit -m "feat: Hero section — centrado cinematográfico + motion system"
```

---

## Task 10: ValueBar, WhatWeDo, Process, Benefits, Cases

**Files:**
- Create: `hat3x-web/components/sections/ValueBar.tsx`
- Create: `hat3x-web/components/sections/WhatWeDo.tsx`
- Create: `hat3x-web/components/sections/Process.tsx`
- Create: `hat3x-web/components/sections/Benefits.tsx`
- Create: `hat3x-web/components/sections/Cases.tsx`

- [ ] **Step 1: Implementar ValueBar**

Crear `hat3x-web/components/sections/ValueBar.tsx`:

```tsx
'use client'

import { useRef, useEffect, useState } from 'react'
import { useInView } from 'framer-motion'
import { VALUE_METRICS } from '@/lib/constants'
import { FadeInUp } from '@/components/motion/FadeInUp'

function CountUp({ target, suffix = '' }: { target: string; suffix?: string }) {
  const [display, setDisplay] = useState('0')
  const ref = useRef<HTMLSpanElement>(null)
  const isInView = useInView(ref, { once: true })
  const numeric = parseInt(target, 10)

  useEffect(() => {
    if (!isInView || isNaN(numeric)) {
      setDisplay(target)
      return
    }
    let start = 0
    const duration = 1500
    const step = duration / numeric
    const timer = setInterval(() => {
      start += 1
      setDisplay(String(start) + suffix)
      if (start >= numeric) {
        setDisplay(target + suffix)
        clearInterval(timer)
      }
    }, step)
    return () => clearInterval(timer)
  }, [isInView, numeric, target, suffix])

  return <span ref={ref}>{display}</span>
}

export function ValueBar() {
  return (
    <section className="relative bg-surface-2 overflow-hidden">
      {/* Top gradient border */}
      <div
        aria-hidden="true"
        className="absolute top-0 left-0 right-0 h-px"
        style={{ background: 'linear-gradient(90deg, transparent, rgba(124,58,237,0.5), rgba(234,88,12,0.3), transparent)' }}
      />

      <div className="max-w-content mx-auto px-6 py-10 grid grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-0">
        {VALUE_METRICS.map((metric, i) => (
          <FadeInUp key={metric.label} delay={i * 0.1} className={`px-6 ${i < VALUE_METRICS.length - 1 ? 'lg:border-r border-border-subtle' : ''}`}>
            <div className="text-3xl font-black text-purple-light tracking-display leading-none mb-1">
              <CountUp target={metric.value} suffix={'suffix' in metric ? metric.suffix : ''} />
            </div>
            <div className="text-xs font-semibold text-text-muted mb-1">{metric.label}</div>
            <div className="text-xs text-text-ghost leading-relaxed">{metric.desc}</div>
          </FadeInUp>
        ))}
      </div>
    </section>
  )
}
```

- [ ] **Step 2: Implementar WhatWeDo**

Crear `hat3x-web/components/sections/WhatWeDo.tsx`:

```tsx
import { SectionLabel } from '@/components/ui/SectionLabel'
import { FadeInUp } from '@/components/motion/FadeInUp'
import { StaggerChildren } from '@/components/motion/StaggerChildren'
import { WHAT_WE_DO } from '@/lib/constants'

export function WhatWeDo() {
  return (
    <section id="nosotros" className="relative bg-surface-1 py-24 overflow-hidden">
      <div className="max-w-content mx-auto px-6">
        <FadeInUp>
          <SectionLabel color="orange" className="mb-4">{WHAT_WE_DO.eyebrow}</SectionLabel>
        </FadeInUp>
        <FadeInUp delay={0.1}>
          <h2 className="text-4xl lg:text-5xl font-black tracking-heading text-white leading-tight mb-6 max-w-xl">
            {WHAT_WE_DO.title}
          </h2>
        </FadeInUp>
        <StaggerChildren className="flex flex-wrap gap-2.5" childClassName="">
          {WHAT_WE_DO.pills.map((pill) => (
            <span
              key={pill}
              className="px-4 py-1.5 text-sm text-text-secondary bg-purple-primary/7 border border-purple-primary/15 rounded-full"
            >
              {pill}
            </span>
          ))}
        </StaggerChildren>
      </div>
    </section>
  )
}
```

- [ ] **Step 3: Implementar Process**

Crear `hat3x-web/components/sections/Process.tsx`:

```tsx
'use client'

import { useRef } from 'react'
import { motion, useInView } from 'framer-motion'
import { SectionLabel } from '@/components/ui/SectionLabel'
import { FadeInUp } from '@/components/motion/FadeInUp'
import { PROCESS_STEPS } from '@/lib/constants'
import { drawLineVariants, fadeInUpVariants } from '@/components/motion/variants'

export function Process() {
  const lineRef = useRef<HTMLDivElement>(null)
  const isInView = useInView(lineRef, { once: true, margin: '-20% 0px' })

  return (
    <section id="proceso" className="relative bg-surface-2 py-24 overflow-hidden">
      <div className="max-w-content mx-auto px-6">
        <FadeInUp>
          <SectionLabel color="orange" className="mb-4">Cómo trabajamos</SectionLabel>
        </FadeInUp>
        <FadeInUp delay={0.1}>
          <h2 className="text-4xl lg:text-5xl font-black tracking-heading text-white mb-16 max-w-sm">
            Así lo hacemos.
          </h2>
        </FadeInUp>

        <div className="relative">
          {/* Connecting line — desktop */}
          <div ref={lineRef} className="hidden lg:block absolute top-[22px] left-[28px] right-[28px] h-px overflow-hidden">
            <motion.div
              variants={drawLineVariants}
              initial="hidden"
              animate={isInView ? 'visible' : 'hidden'}
              className="h-full origin-left"
              style={{ background: 'linear-gradient(90deg, #7C3AED, rgba(234,88,12,0.5))' }}
            />
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-5 gap-8 lg:gap-4">
            {PROCESS_STEPS.map((step, i) => (
              <motion.div
                key={step.num}
                className="flex flex-col items-center lg:items-start gap-3 text-center lg:text-left"
                variants={fadeInUpVariants}
                initial="hidden"
                animate={isInView ? 'visible' : 'hidden'}
                transition={{ delay: 0.3 + i * 0.12 }}
              >
                <div className="relative z-10 w-11 h-11 rounded-full flex items-center justify-center border border-purple-primary bg-surface-2 text-xs font-black text-purple-light">
                  {step.num}
                </div>
                <div>
                  <p className="text-sm font-bold text-text-primary mb-1">{step.label}</p>
                  <p className="text-xs text-text-ghost leading-relaxed">{step.desc}</p>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Implementar Benefits**

Crear `hat3x-web/components/sections/Benefits.tsx`:

```tsx
import { SectionLabel } from '@/components/ui/SectionLabel'
import { FadeInUp } from '@/components/motion/FadeInUp'
import { StaggerChildren } from '@/components/motion/StaggerChildren'
import { BENEFITS } from '@/lib/constants'

export function Benefits() {
  return (
    <section className="relative bg-surface-1 py-24 overflow-hidden">
      <div
        aria-hidden="true"
        className="absolute inset-0 pointer-events-none"
        style={{
          background: 'linear-gradient(135deg, rgba(124,58,237,0.04) 0%, rgba(234,88,12,0.02) 100%)',
        }}
      />
      <div className="max-w-content mx-auto px-6 relative">
        <FadeInUp>
          <SectionLabel color="purple" className="mb-4">Por qué HAT3X</SectionLabel>
        </FadeInUp>
        <FadeInUp delay={0.1}>
          <h2 className="text-4xl lg:text-5xl font-black tracking-heading text-white mb-12 max-w-sm">
            Por qué HAT3X.
          </h2>
        </FadeInUp>

        <StaggerChildren className="grid grid-cols-2 lg:grid-cols-4 gap-4" childClassName="">
          {BENEFITS.map((benefit) => (
            <div key={benefit} className="flex items-start gap-3">
              <div className="w-1.5 h-1.5 rounded-full bg-purple-primary flex-shrink-0 mt-1.5" />
              <p className="text-sm text-text-secondary leading-relaxed">{benefit}</p>
            </div>
          ))}
        </StaggerChildren>
      </div>
    </section>
  )
}
```

- [ ] **Step 5: Implementar Cases**

Crear `hat3x-web/components/sections/Cases.tsx`:

```tsx
import { SectionLabel } from '@/components/ui/SectionLabel'
import { FadeInUp } from '@/components/motion/FadeInUp'
import { StaggerChildren } from '@/components/motion/StaggerChildren'
import { CASES } from '@/lib/constants'

export function Cases() {
  return (
    <section id="casos" className="relative bg-surface-2 py-24 overflow-hidden">
      <div className="max-w-content mx-auto px-6">
        <FadeInUp>
          <SectionLabel color="orange" className="mb-4">Sectores que transformamos</SectionLabel>
        </FadeInUp>
        <FadeInUp delay={0.1}>
          <h2 className="text-4xl lg:text-5xl font-black tracking-heading text-white mb-12 max-w-md">
            Resultados reales en tu sector.
          </h2>
        </FadeInUp>

        <StaggerChildren className="grid grid-cols-1 md:grid-cols-3 gap-4" childClassName="">
          {CASES.map((c) => (
            <div
              key={c.sector}
              className="bg-white/[0.02] border border-border-subtle hover:border-purple-primary/30 rounded-xl p-6 flex flex-col gap-3 transition-all duration-250 group"
            >
              <p className="text-[0.625rem] font-bold uppercase tracking-eyebrow text-purple-light">{c.sector}</p>
              <p className="text-3xl font-black tracking-display text-purple-primary group-hover:text-purple-light transition-colors">{c.metric}</p>
              <p className="text-sm font-semibold text-text-primary">{c.title}</p>
              <p className="text-xs text-text-ghost leading-relaxed">{c.desc}</p>
            </div>
          ))}
        </StaggerChildren>
      </div>
    </section>
  )
}
```

- [ ] **Step 6: Commit**

```bash
git add components/sections/ValueBar.tsx components/sections/WhatWeDo.tsx components/sections/Process.tsx components/sections/Benefits.tsx components/sections/Cases.tsx
git commit -m "feat: ValueBar, WhatWeDo, Process, Benefits, Cases sections"
```

---

## Task 11: Services — Bento Grid

**Files:**
- Create: `hat3x-web/components/sections/Services.tsx`
- Create: `hat3x-web/__tests__/sections/Services.test.tsx`

- [ ] **Step 1: Escribir test de Services**

Crear `hat3x-web/__tests__/sections/Services.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { Services } from '@/components/sections/Services'

describe('Services', () => {
  it('renders all 6 services', () => {
    render(<Services />)
    expect(screen.getByText('Auditoría y Roadmap de IA')).toBeInTheDocument()
    expect(screen.getByText('Automatización y Agentes')).toBeInTheDocument()
    expect(screen.getByText('Apps y Productos con IA')).toBeInTheDocument()
    expect(screen.getByText('Integraciones')).toBeInTheDocument()
    expect(screen.getByText('Analítica y ML')).toBeInTheDocument()
    expect(screen.getByText('Gobernanza y Adopción')).toBeInTheDocument()
  })

  it('renders section heading', () => {
    render(<Services />)
    expect(screen.getByText('Lo que implementamos.')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Ejecutar test — verificar que falla**

```bash
npm run test -- __tests__/sections/Services.test.tsx
```

Esperado: FAIL

- [ ] **Step 3: Implementar Services (Bento Grid)**

Crear `hat3x-web/components/sections/Services.tsx`:

```tsx
'use client'

import { motion } from 'framer-motion'
import { SectionLabel } from '@/components/ui/SectionLabel'
import { FadeInUp } from '@/components/motion/FadeInUp'
import { StaggerChildren } from '@/components/motion/StaggerChildren'
import { SERVICES } from '@/lib/constants'
import { cn } from '@/lib/utils'

function ServiceCard({ service }: { service: typeof SERVICES[number] }) {
  return (
    <div
      className={cn(
        'relative group rounded-xl border p-5 transition-all duration-250 overflow-hidden',
        'bg-white/[0.02] border-border-subtle',
        'hover:border-purple-primary/30 hover:bg-purple-primary/[0.04]',
        service.featured && 'md:col-span-2 bg-purple-primary/[0.04] border-purple-primary/15'
      )}
    >
      {/* Top gradient line on hover */}
      <div
        aria-hidden="true"
        className="absolute top-0 left-0 right-0 h-px opacity-0 group-hover:opacity-100 transition-opacity duration-250"
        style={{ background: 'linear-gradient(90deg, transparent, rgba(124,58,237,0.4), transparent)' }}
      />
      {service.featured && (
        <div
          aria-hidden="true"
          className="absolute top-0 left-0 right-0 h-px opacity-50"
          style={{ background: 'linear-gradient(90deg, transparent, rgba(124,58,237,0.4), transparent)' }}
        />
      )}

      {/* Number */}
      <span className="absolute top-4 right-4 text-[0.6rem] font-bold text-text-ghost">{service.id}</span>

      {/* Icon */}
      <div className="w-9 h-9 rounded-lg bg-purple-primary/12 border border-purple-primary/20 flex items-center justify-center text-base mb-4">
        {service.icon}
      </div>

      {/* Content */}
      <h3 className="text-base font-bold text-text-primary tracking-card mb-2">{service.title}</h3>
      <p className="text-xs text-text-muted leading-relaxed mb-3">{service.desc}</p>

      {/* Tags */}
      {service.featured ? (
        <div className="flex flex-wrap gap-2">
          {service.tags.map((tag) => (
            <span key={tag} className="text-[0.6rem] text-text-muted bg-white/[0.03] border border-border-subtle px-2.5 py-1 rounded-full">
              {tag}
            </span>
          ))}
        </div>
      ) : (
        <span className="text-[0.6rem] text-purple-light bg-purple-primary/6 border border-purple-primary/12 px-2.5 py-1 rounded">
          {service.tags[0]}
        </span>
      )}
    </div>
  )
}

export function Services() {
  return (
    <section id="servicios" className="relative bg-surface-2 py-24 overflow-hidden">
      <div className="max-w-content mx-auto px-6">
        <FadeInUp>
          <SectionLabel color="purple" className="mb-4">Servicios</SectionLabel>
        </FadeInUp>
        <FadeInUp delay={0.1}>
          <h2 className="text-4xl lg:text-5xl font-black tracking-heading text-white mb-3 max-w-sm">
            Lo que implementamos.
          </h2>
        </FadeInUp>
        <FadeInUp delay={0.15}>
          <p className="text-sm text-text-muted mb-12 max-w-md leading-relaxed">
            Seis verticales de IA. Cada una diseñada para entregar resultados medibles desde el primer mes.
          </p>
        </FadeInUp>

        <StaggerChildren
          className="grid grid-cols-1 md:grid-cols-3 gap-3"
          childClassName=""
        >
          {SERVICES.map((service) => (
            <ServiceCard key={service.id} service={service} />
          ))}
        </StaggerChildren>
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Ejecutar tests**

```bash
npm run test -- __tests__/sections/Services.test.tsx
```

Esperado: PASS

- [ ] **Step 5: Commit**

```bash
git add components/sections/Services.tsx __tests__/sections/Services.test.tsx
git commit -m "feat: Services — Bento Grid con card featured + hover states"
```

---

## Task 12: CTA Final y Footer

**Files:**
- Create: `hat3x-web/components/sections/CtaFinal.tsx`
- Create: `hat3x-web/components/sections/Footer.tsx`
- Create: `hat3x-web/__tests__/sections/CtaFinal.test.tsx`

- [ ] **Step 1: Escribir test de CtaFinal**

Crear `hat3x-web/__tests__/sections/CtaFinal.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { CtaFinal } from '@/components/sections/CtaFinal'

describe('CtaFinal', () => {
  it('renders headline', () => {
    render(<CtaFinal />)
    expect(screen.getByText(/implementar ia real/i)).toBeInTheDocument()
  })

  it('renders primary CTA link', () => {
    render(<CtaFinal />)
    const cta = screen.getByRole('link', { name: /reservar llamada/i })
    expect(cta).toBeInTheDocument()
  })

  it('renders secondary CTA', () => {
    render(<CtaFinal />)
    expect(screen.getByRole('link', { name: /contactar por email/i })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Implementar CtaFinal**

Crear `hat3x-web/components/sections/CtaFinal.tsx`:

```tsx
'use client'

import Link from 'next/link'
import { FadeInUp } from '@/components/motion/FadeInUp'
import { GlowOrb } from '@/components/decorative/GlowOrb'
import { CTA_FINAL } from '@/lib/constants'

export function CtaFinal() {
  return (
    <section className="relative bg-base-black py-32 overflow-hidden">
      {/* Background glow */}
      <GlowOrb color="purple" size={500} opacity={0.25} blur={80} className="bottom-[-100px] left-1/2 -translate-x-1/2" />

      <div className="max-w-content mx-auto px-6 relative z-10 flex flex-col items-center text-center gap-6">
        <FadeInUp>
          <h2 className="text-4xl lg:text-5xl xl:text-6xl font-black tracking-heading text-white leading-tight max-w-lg">
            {CTA_FINAL.title.split('IA real').map((part, i) => (
              i === 0
                ? <span key={i}>{part}<span className="text-purple-light">IA real</span></span>
                : <span key={i}>{part}</span>
            ))}
          </h2>
        </FadeInUp>

        <FadeInUp delay={0.1}>
          <p className="text-sm text-text-muted">
            {CTA_FINAL.subtitle}
          </p>
        </FadeInUp>

        <FadeInUp delay={0.2}>
          <div className="flex flex-col sm:flex-row gap-3 mt-2">
            <Link
              href={CTA_FINAL.ctaPrimaryHref}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center justify-center gap-2 bg-purple-primary hover:bg-[#6D28D9] text-white text-sm font-bold px-8 py-3.5 rounded-lg transition-all duration-200 shadow-[0_4px_24px_rgba(124,58,237,0.45),0_0_60px_rgba(124,58,237,0.1)] hover:shadow-[0_4px_32px_rgba(124,58,237,0.6),0_0_80px_rgba(124,58,237,0.15)] animate-pulse-glow"
            >
              {CTA_FINAL.ctaPrimary} →
            </Link>
            <Link
              href={CTA_FINAL.ctaSecondaryHref}
              className="inline-flex items-center justify-center gap-2 border border-border-subtle hover:border-[#2a2a3a] text-text-secondary hover:text-text-primary text-sm font-semibold px-8 py-3.5 rounded-lg transition-all duration-200"
            >
              {CTA_FINAL.ctaSecondary}
            </Link>
          </div>
        </FadeInUp>
      </div>
    </section>
  )
}
```

- [ ] **Step 3: Implementar Footer**

Crear `hat3x-web/components/sections/Footer.tsx`:

```tsx
import Link from 'next/link'
import { FOOTER } from '@/lib/constants'

export function Footer() {
  return (
    <footer className="bg-[#030308] border-t border-border-subtle">
      <div
        aria-hidden="true"
        className="h-px"
        style={{ background: 'linear-gradient(90deg, transparent, rgba(124,58,237,0.2), transparent)' }}
      />
      <div className="max-w-content mx-auto px-6 py-10 flex flex-col md:flex-row items-center gap-4">
        <div className="flex flex-col md:flex-row items-center gap-2">
          <span className="text-sm font-black text-text-ghost tracking-card">HAT3X</span>
          <span className="hidden md:inline text-text-ghost">·</span>
          <span className="text-xs text-text-ghost">{FOOTER.tagline}</span>
        </div>

        <nav className="flex items-center gap-6 md:ml-auto" aria-label="Footer navigation">
          {FOOTER.links.map((link) => (
            <Link
              key={link.href}
              href={link.href}
              className="text-xs text-text-ghost hover:text-text-secondary transition-colors"
            >
              {link.label}
            </Link>
          ))}
        </nav>

        <p className="text-xs text-text-ghost md:ml-6">{FOOTER.copy}</p>
      </div>
    </footer>
  )
}
```

- [ ] **Step 4: Ejecutar tests**

```bash
npm run test -- __tests__/sections/CtaFinal.test.tsx
```

Esperado: PASS

- [ ] **Step 5: Commit**

```bash
git add components/sections/CtaFinal.tsx components/sections/Footer.tsx __tests__/sections/CtaFinal.test.tsx
git commit -m "feat: CtaFinal glow pulsante + Footer minimal dark"
```

---

## Task 13: Ensamblaje de la homepage

**Files:**
- Modify: `hat3x-web/app/page.tsx`

- [ ] **Step 1: Reemplazar page.tsx con el ensamblaje completo**

Reemplazar `hat3x-web/app/page.tsx`:

```tsx
import { Nav } from '@/components/sections/Nav'
import { Hero } from '@/components/sections/Hero'
import { ValueBar } from '@/components/sections/ValueBar'
import { WhatWeDo } from '@/components/sections/WhatWeDo'
import { Services } from '@/components/sections/Services'
import { Process } from '@/components/sections/Process'
import { Benefits } from '@/components/sections/Benefits'
import { Cases } from '@/components/sections/Cases'
import { CtaFinal } from '@/components/sections/CtaFinal'
import { Footer } from '@/components/sections/Footer'

export default function Home() {
  return (
    <>
      <Nav />
      <main>
        <Hero />
        <ValueBar />
        <WhatWeDo />
        <Services />
        <Process />
        <Benefits />
        <Cases />
        <CtaFinal />
      </main>
      <Footer />
    </>
  )
}
```

- [ ] **Step 2: Verificar en el navegador**

```bash
npm run dev
```

Abrir `http://localhost:3000` y verificar:
- Hero visible con glow y grid
- Nav sticky al hacer scroll
- Todas las secciones cargan sin errores en consola

- [ ] **Step 3: Ejecutar todos los tests**

```bash
npm run test
```

Esperado: todos los tests PASS, 0 fallos.

- [ ] **Step 4: Build de producción**

```bash
npm run build
```

Esperado: build exitoso. Revisar Output y corregir cualquier warning de TypeScript.

- [ ] **Step 5: Commit**

```bash
git add app/page.tsx
git commit -m "feat: homepage — ensamblaje completo de todas las secciones"
```

---

## Task 14: Stitch MCP — Generación visual de refinamientos

**Objetivo:** Usar el MCP de Stitch para generar variantes visuales de las secciones más críticas y exportar el código de componentes refinados.

- [ ] **Step 1: Crear proyecto Stitch para HAT3X**

Invocar `mcp__stitch__create_project` con título `"HAT3X Web Premium"`.

- [ ] **Step 2: Generar Hero screen en Stitch**

Invocar `mcp__stitch__generate_screen_from_text` con prompt:

```
Dark premium hero section for HAT3X, an AI consulting firm.
Background: deep black (#040408) with purple radial glow from top center.
Purple tech grid lines pattern (very subtle, 5% opacity).
Center content: small pill badge "Implementación IA para empresas" with orange pulsing dot.
Large headline in two lines: "No aprendes IA." (white) / "HAT3X la implementa." (gradient purple to orange).
Subtitle: small gray text, max 380px wide.
Two CTAs: primary purple button "Reservar llamada gratuita →", secondary ghost button "Ver servicios".
Floating node dots in corners connected by faint lines.
Orange glow orb bottom-right corner.
Style: ultra-premium, dark, Vercel-like quality.
```

- [ ] **Step 3: Generar Services screen en Stitch**

Invocar `mcp__stitch__generate_screen_from_text` con prompt:

```
Dark premium services section bento grid for HAT3X AI consulting.
Background: dark (#0B0B16).
Section eyebrow label "Servicios" in purple uppercase.
Large title "Lo que implementamos." in white.
3-column bento grid, 6 service cards total.
First card spans 2 columns (featured): "Auditoría y Roadmap de IA" with longer description and metadata pills.
Other 5 cards: single column, each with icon, title, description, tag pill.
Card style: subtle dark background, 1px border, purple glow on hover.
All dark premium, minimal borders, purple/orange accent system.
```

- [ ] **Step 4: Revisar outputs de Stitch**

Invocar `mcp__stitch__list_screens` para el proyecto creado y revisar los resultados generados.

- [ ] **Step 5: Aplicar refinamientos visuales identificados**

Comparar outputs de Stitch con implementación actual. Aplicar mejoras de:
- Espaciados y proporciones
- Intensidad de glows y gradientes
- Densidad del grid tecnológico

- [ ] **Step 6: Commit de refinamientos**

```bash
git add -A
git commit -m "feat: refinamientos visuales aplicados desde Stitch MCP"
```

---

## Task 15: Deploy en Vercel

**Files:**
- Create: `hat3x-web/vercel.json`

- [ ] **Step 1: Crear vercel.json**

Crear `hat3x-web/vercel.json`:

```json
{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
```

- [ ] **Step 2: Actualizar URLs reales en constants.ts**

En `hat3x-web/lib/constants.ts`, reemplazar los placeholders:
- `ctaPrimaryHref`: URL real de Cal.com/Calendly de HAT3X
- `ctaSecondaryHref`: Email real de contacto
- `FOOTER.copy`: Año correcto

- [ ] **Step 3: Deploy a Vercel**

```bash
npx vercel --prod
```

O conectar el repositorio desde el dashboard de Vercel en `vercel.com/new`.

- [ ] **Step 4: Verificar métricas de rendimiento**

Una vez desplegado, ejecutar Lighthouse en la URL de producción:

```bash
npx lighthouse https://hat3x-web.vercel.app --output html --output-path lighthouse-report.html
```

Objetivos:
- Performance: > 90
- Accessibility: > 90
- Best Practices: > 90
- SEO: > 90

- [ ] **Step 5: Commit final**

```bash
git add vercel.json
git commit -m "feat: Vercel deploy config + headers de seguridad"
```

---

## Self-Review del plan

**Cobertura del spec:**

| Req. spec | Task que lo cubre |
|---|---|
| Dark premium, fondo #040408 | Task 2, 9 |
| Paleta violeta + naranja con tokens | Task 2 |
| Tipografía Inter con tracking compacto | Task 3, 2 |
| Motion system con Framer Motion | Task 5 |
| Nav glassmorphism scroll-aware | Task 8 |
| Hero centrado cinematográfico | Task 9 |
| ValueBar con counter animation | Task 10 |
| Qué hacemos con pills | Task 10 |
| Services Bento Grid 3×2 | Task 11 |
| Process timeline animado | Task 10 |
| Benefits grid 4×2 | Task 10 |
| Cases 3 cards por sector | Task 10 |
| CTA Final con glow pulsante | Task 12 |
| Footer minimal | Task 12 |
| Ensamblaje homepage | Task 13 |
| Stitch MCP refinamientos | Task 14 |
| Vercel deploy | Task 15 |
| Copy en constants.ts (mantenible) | Task 4 |
| Responsive mobile/tablet | Todas (clases sm:/md:/lg:) |
| LCP < 2.5s, Lighthouse > 90 | Task 15 |
| Tests de componentes críticos | Tasks 5, 6, 8, 9, 11, 12 |

**Sin placeholders:** Confirmado — todo el código está completo en cada step.

**Consistencia de tipos:** `SERVICES` usa `as const`, el tipo `typeof SERVICES[number]` en `ServiceCard` es correcto. `VALUE_METRICS` con `'suffix' in metric` maneja el campo opcional correctamente.

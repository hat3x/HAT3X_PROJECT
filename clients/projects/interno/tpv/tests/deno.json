{
  "//": "Config de la suite de tests del TPV. Usa el import map de tests (stub de Supabase) para correr integración y e2e sin red. Ejecutar desde tpv/tests: `deno task test`.",
  "importMap": "./import_map.test.json",
  "fmt": {
    "singleQuote": true,
    "lineWidth": 90
  },
  "lint": {
    "rules": { "tags": ["recommended"] }
  },
  "tasks": {
    "test": "deno test --allow-none",
    "test:watch": "deno test --allow-none --watch",
    "cov": "deno test --allow-none --coverage=.coverage && deno coverage .coverage"
  }
}
